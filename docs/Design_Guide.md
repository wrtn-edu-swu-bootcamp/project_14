# 폴리 AI 디자인 가이드

> **버전**: 1.0  
> **작성일**: 2026년 1월  
> **담당**: 브랜드 디자인팀

---

## 📋 목차

1. [브랜드 아이덴티티](#1-브랜드-아이덴티티)
2. [컬러 시스템](#2-컬러-시스템)
3. [타이포그래피](#3-타이포그래피)
4. [아이콘 시스템](#4-아이콘-시스템)
5. [그리드 시스템](#5-그리드-시스템)
6. [컴포넌트 스타일](#6-컴포넌트-스타일)
7. [모션 디자인](#7-모션-디자인)
8. [일러스트레이션](#8-일러스트레이션)
9. [다크 모드](#9-다크-모드)

---

## 1. 브랜드 아이덴티티

### 1.1 브랜드 철학

**"청년과 정책을 친근하게 연결하다"**

폴리 AI는 복잡하고 어려운 정부 정책을 청년들이 쉽고 친근하게 접할 수 있도록 돕는 서비스입니다. 우리의 디자인은 다음 3가지 핵심 가치를 반영합니다:

#### 친근함 (Friendly)
- 반말 톤앤매너
- 부드러운 곡선과 둥근 모서리
- 따뜻한 색상 조합
- 이모지 활용

#### 신뢰성 (Trustworthy)
- 명확한 정보 구조
- 일관된 UI 패턴
- 공식 출처 표시
- 깔끔한 여백

#### 혁신성 (Innovative)
- AI 기술 강조
- 그라데이션 활용
- 현대적인 인터페이스
- 스무스한 애니메이션

### 1.2 브랜드 네이밍

**폴리 (Policy) AI**

- **의미**: Policy(정책) + AI(인공지능)
- **발음**: 폴리 (쉽고 친근한 발음)
- **연상**: "친구 같은 정책 도우미"

### 1.3 로고 디자인

#### 로고 컨셉

**심볼**: Sparkles 아이콘 (✨)
- AI의 똑똑함과 혁신성 표현
- 정책을 찾는 순간의 '반짝임' 표현

**워드마크**: "폴리 AI"
- 폰트: Inter, Bold
- 그라데이션: Indigo → Purple

#### 로고 사용 규정

**최소 사이즈**:
- 디지털: 120px (너비)
- 인쇄물: 30mm (너비)

**클리어 스페이스**:
- 로고 높이의 50%를 사방으로 확보

**금지 사항**:
- ❌ 로고 비율 변경
- ❌ 로고 색상 임의 변경
- ❌ 로고 회전
- ❌ 로고에 효과 추가

---

## 2. 컬러 시스템

### 2.1 브랜드 컬러

#### Primary: Indigo

청년 정책의 신뢰성과 공공성을 표현하는 메인 컬러

| 이름 | Hex | RGB | 용도 |
|-----|-----|-----|------|
| Indigo-50 | `#EEF2FF` | rgb(238, 242, 255) | 배경, 강조 영역 |
| Indigo-100 | `#E0E7FF` | rgb(224, 231, 255) | Hover 배경 |
| Indigo-200 | `#C7D2FE` | rgb(199, 210, 254) | 테두리 |
| Indigo-400 | `#818CF8` | rgb(129, 140, 248) | 비활성화 |
| Indigo-500 | `#6366F1` | rgb(99, 102, 241) | 보조 액션 |
| Indigo-600 | `#4F46E5` | rgb(79, 70, 229) | **주요 액션** |
| Indigo-700 | `#4338CA` | rgb(67, 56, 202) | Hover 상태 |
| Indigo-800 | `#3730A3` | rgb(55, 48, 163) | 텍스트 |
| Indigo-900 | `#312E81` | rgb(49, 46, 129) | 강한 텍스트 |

**사용 예시**:
- 버튼 배경 (Indigo-600)
- 링크 (Indigo-600)
- 아이콘 (Indigo-500)

#### Secondary: Purple

AI와 혁신을 상징하는 보조 컬러

| 이름 | Hex | RGB | 용도 |
|-----|-----|-----|------|
| Purple-50 | `#FAF5FF` | rgb(250, 245, 255) | 배경 |
| Purple-100 | `#F3E8FF` | rgb(243, 232, 255) | 뱃지 배경 |
| Purple-400 | `#A78BFA` | rgb(167, 139, 250) | 타이핑 인디케이터 |
| Purple-500 | `#8B5CF6` | rgb(139, 92, 246) | AI 관련 요소 |
| Purple-600 | `#7C3AED` | rgb(124, 58, 237) | **그라데이션 중간** |
| Purple-700 | `#6D28D9` | rgb(109, 40, 217) | Hover |
| Purple-800 | `#5B21B6` | rgb(91, 33, 182) | 텍스트 |

**사용 예시**:
- AI 메시지 아바타
- RAG 뱃지
- 그라데이션 요소

#### Accent: Pink

친근함과 따뜻함을 더하는 액센트 컬러

| 이름 | Hex | RGB | 용도 |
|-----|-----|-----|------|
| Pink-50 | `#FDF2F8` | rgb(253, 242, 248) | 배경 |
| Pink-100 | `#FCE7F3` | rgb(252, 231, 243) | 강조 영역 |
| Pink-400 | `#F472B6` | rgb(244, 114, 182) | 타이핑 인디케이터 |
| Pink-500 | `#EC4899` | rgb(236, 72, 153) | 액센트 |
| Pink-600 | `#DB2777` | rgb(219, 39, 119) | **그라데이션 끝** |

**사용 예시**:
- 그라데이션 끝 색상
- 카테고리 뱃지 (자산형성)

### 2.2 기능 컬러

#### Success: Green

성공, 완료, 긍정적 메시지

| 이름 | Hex | 용도 |
|-----|-----|------|
| Green-50 | `#F0FDF4` | 배경 |
| Green-100 | `#DCFCE7` | 뱃지 배경 |
| Green-600 | `#16A34A` | 텍스트, 아이콘 |
| Green-700 | `#15803D` | 진한 텍스트 |

**사용 예시**:
- 관련도 점수 뱃지
- Notion 저장 완료 메시지
- 체크 아이콘

#### Warning: Yellow/Orange

주의, 확인 필요

| 이름 | Hex | 용도 |
|-----|-----|------|
| Yellow-50 | `#FFFBEB` | 배경 |
| Yellow-100 | `#FEF3C7` | 뱃지 배경 |
| Orange-500 | `#F97316` | 경고 아이콘 |
| Orange-600 | `#EA580C` | 경고 텍스트 |

**사용 예시**:
- 교육 카테고리 뱃지
- 주의 메시지

#### Error: Red

오류, 실패, 부정적 메시지

| 이름 | Hex | 용도 |
|-----|-----|------|
| Red-50 | `#FEF2F2` | 배경 |
| Red-200 | `#FECACA` | 테두리 |
| Red-600 | `#DC2626` | 텍스트, 아이콘 |
| Red-800 | `#991B1B` | 진한 텍스트 |

**사용 예시**:
- 에러 메시지
- 필수 입력 표시

#### Info: Blue

정보, 안내

| 이름 | Hex | 용도 |
|-----|-----|------|
| Blue-50 | `#EFF6FF` | 배경 |
| Blue-100 | `#DBEAFE` | 뱃지 배경 |
| Blue-600 | `#2563EB` | 텍스트, 아이콘 |
| Blue-800 | `#1E40AF` | 진한 텍스트 |

**사용 예시**:
- Notion 설정 안내
- 주거 카테고리 뱃지
- 정보 메시지

### 2.3 중립 컬러 (Neutrals)

#### Gray Scale

| 이름 | Hex | 용도 |
|-----|-----|------|
| White | `#FFFFFF` | 배경, 카드 |
| Gray-50 | `#F9FAFB` | 연한 배경 |
| Gray-100 | `#F3F4F6` | 비활성화 배경 |
| Gray-200 | `#E5E7EB` | 테두리 |
| Gray-300 | `#D1D5DB` | 구분선 |
| Gray-400 | `#9CA3AF` | 플레이스홀더 |
| Gray-500 | `#6B7280` | 부연 설명 |
| Gray-600 | `#4B5563` | 보조 텍스트 |
| Gray-700 | `#374151` | 본문 텍스트 |
| Gray-800 | `#1F2937` | 제목 텍스트 |
| Gray-900 | `#111827` | 강조 텍스트 |
| Black | `#000000` | 최고 강조 |

### 2.4 그라데이션

#### Primary Gradient

**Indigo → Purple → Pink**

```css
background: linear-gradient(
  135deg,
  #6366F1 0%,    /* Indigo-600 */
  #8B5CF6 50%,   /* Purple-500 */
  #EC4899 100%   /* Pink-500 */
);
```

**사용 예시**:
- 주요 CTA 버튼
- AI 아바타 배경
- 로고

#### Background Gradient

**페이지 배경 (연한 그라데이션)**

```css
background: linear-gradient(
  135deg,
  #EEF2FF 0%,    /* Indigo-50 */
  #FAF5FF 50%,   /* Purple-50 */
  #FDF2F8 100%   /* Pink-50 */
);
```

### 2.5 카테고리별 컬러

| 카테고리 | 배경 | 텍스트 | 이모지 |
|---------|------|--------|--------|
| 주거 | Blue-100 | Blue-800 | 🏠 |
| 일자리 | Green-100 | Green-800 | 💼 |
| 창업 | Purple-100 | Purple-800 | 🚀 |
| 교육 | Yellow-100 | Yellow-800 | 📚 |
| 자산형성 | Pink-100 | Pink-800 | 💰 |
| 복지 | Teal-100 | Teal-800 | 🩺 |
| 문화 | Orange-100 | Orange-800 | 🎨 |
| 기타 | Gray-100 | Gray-800 | 📋 |

---

## 3. 타이포그래피

### 3.1 폰트 패밀리

#### Primary Font: Inter

**웹 폰트**:
```css
@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap');

font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
```

**특징**:
- 가독성이 뛰어난 산세리프
- 숫자가 깔끔하게 표현됨 (정책 금액 표시에 유리)
- 다양한 웨이트 제공

#### Fallback Fonts

```css
font-family: 'Inter', -apple-system, BlinkMacSystemFont, 
  'Segoe UI', 'Helvetica Neue', Arial, sans-serif,
  'Apple Color Emoji', 'Segoe UI Emoji', 'Segoe UI Symbol';
```

### 3.2 타이포그래피 스케일

#### Heading

| 스타일 | 크기 | 웨이트 | 행간 | 용도 |
|-------|------|--------|------|------|
| H1 | 40px | 800 (ExtraBold) | 48px | 페이지 타이틀 |
| H2 | 32px | 700 (Bold) | 40px | 섹션 제목 |
| H3 | 24px | 700 (Bold) | 32px | 카드 제목 |
| H4 | 20px | 600 (SemiBold) | 28px | 서브 제목 |
| H5 | 18px | 600 (SemiBold) | 24px | 작은 제목 |
| H6 | 16px | 600 (SemiBold) | 24px | 캡션 제목 |

**사용 예시**:
```css
.h1 {
  font-size: 40px;
  font-weight: 800;
  line-height: 48px;
  letter-spacing: -0.02em;
}
```

#### Body

| 스타일 | 크기 | 웨이트 | 행간 | 용도 |
|-------|------|--------|------|------|
| Body-Large | 18px | 400 (Regular) | 28px | 소개 문구 |
| Body | 16px | 400 (Regular) | 24px | 본문 (기본) |
| Body-Medium | 15px | 400 (Regular) | 24px | 채팅 메시지 |
| Body-Small | 14px | 400 (Regular) | 20px | 보조 설명 |
| Caption | 12px | 500 (Medium) | 16px | 라벨, 뱃지 |

**사용 예시**:
```css
.body {
  font-size: 16px;
  font-weight: 400;
  line-height: 24px;
  letter-spacing: 0;
}
```

### 3.3 폰트 웨이트

| 웨이트 | 값 | 용도 |
|--------|---|------|
| Regular | 400 | 본문, 일반 텍스트 |
| Medium | 500 | 강조 텍스트, 라벨 |
| SemiBold | 600 | 버튼, 제목 |
| Bold | 700 | 카드 제목, 중요 정보 |
| ExtraBold | 800 | 페이지 타이틀, 로고 |

### 3.4 Line Height & Letter Spacing

#### Line Height 기준

```css
--line-height-tight: 1.25;   /* 제목용 */
--line-height-normal: 1.5;   /* 본문용 */
--line-height-loose: 1.75;   /* 여유있는 텍스트 */
```

#### Letter Spacing

```css
--letter-spacing-tight: -0.02em;  /* 큰 제목 */
--letter-spacing-normal: 0;       /* 기본 */
--letter-spacing-wide: 0.02em;    /* 버튼 텍스트 */
```

### 3.5 텍스트 색상

| 용도 | 컬러 | 설명 |
|------|------|------|
| Primary | Gray-900 | 주요 텍스트 (제목, 본문) |
| Secondary | Gray-600 | 보조 텍스트 (부연 설명) |
| Tertiary | Gray-500 | 약한 텍스트 (메타 정보) |
| Placeholder | Gray-400 | 플레이스홀더 |
| Inverse | White | 어두운 배경 위 텍스트 |
| Link | Indigo-600 | 링크, 클릭 가능 텍스트 |
| Success | Green-600 | 성공 메시지 |
| Error | Red-600 | 오류 메시지 |

---

## 4. 아이콘 시스템

### 4.1 아이콘 라이브러리

**Lucide React** 사용

- 스타일: Outline (선 기반)
- 일관성: 모든 아이콘 동일한 라이브러리 사용
- 크기: 16px, 20px, 24px, 32px

### 4.2 아이콘 크기

| 크기 | 용도 |
|------|------|
| 16px | 텍스트 인라인, 작은 버튼 |
| 20px | 버튼, 입력창 내부 |
| 24px | 카드 아이콘, 메뉴 |
| 32px | 대형 아이콘, 강조 |
| 48px | 피처 아이콘, 일러스트 |

### 4.3 아이콘 색상

```css
/* Default */
.icon-default {
  color: var(--gray-600);
}

/* Primary */
.icon-primary {
  color: var(--indigo-600);
}

/* Success */
.icon-success {
  color: var(--green-600);
}

/* Warning */
.icon-warning {
  color: var(--orange-500);
}

/* Error */
.icon-error {
  color: var(--red-600);
}
```

### 4.4 주요 아이콘 목록

| 아이콘 | 이름 | 용도 |
|--------|------|------|
| ✨ | Sparkles | 로고, AI 표시 |
| 🤖 | Bot | AI 메시지 |
| 👤 | User | 사용자 메시지 |
| 📤 | Send | 전송 버튼 |
| ⏳ | Loader2 | 로딩 |
| 🏠 | Home | 주거 카테고리 |
| 💼 | Briefcase | 일자리 카테고리 |
| 📅 | Calendar | 날짜 정보 |
| 📍 | MapPin | 지역 정보 |
| 🏢 | Building2 | 기관 정보 |
| ✓ | CheckCircle2 | 완료, 체크 |
| 🔗 | ExternalLink | 외부 링크 |
| 📚 | BookMarked | Notion 연동 |

---

## 5. 그리드 시스템

### 5.1 컨테이너

| 브레이크포인트 | 최대 너비 | 패딩 |
|---------------|----------|------|
| Mobile (< 640px) | 100% | 16px |
| Tablet (640px - 1024px) | 768px | 24px |
| Desktop (> 1024px) | 1200px | 32px |

```css
.container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 32px;
}

@media (max-width: 1024px) {
  .container {
    max-width: 768px;
    padding: 0 24px;
  }
}

@media (max-width: 640px) {
  .container {
    padding: 0 16px;
  }
}
```

### 5.2 컬럼 그리드

**12 컬럼 그리드 시스템**

```css
.grid {
  display: grid;
  grid-template-columns: repeat(12, 1fr);
  gap: 24px;
}

/* 예시: 2x2 카드 */
.col-6 {
  grid-column: span 6;
}

@media (max-width: 768px) {
  .col-6 {
    grid-column: span 12;
  }
}
```

### 5.3 Spacing Scale

| 이름 | 값 | 용도 |
|------|---|------|
| xs | 4px | 아주 작은 간격 |
| sm | 8px | 작은 간격 |
| md | 16px | 기본 간격 |
| lg | 24px | 큰 간격 |
| xl | 32px | 섹션 간격 |
| 2xl | 48px | 대형 섹션 간격 |
| 3xl | 64px | 페이지 섹션 |

```css
:root {
  --spacing-xs: 4px;
  --spacing-sm: 8px;
  --spacing-md: 16px;
  --spacing-lg: 24px;
  --spacing-xl: 32px;
  --spacing-2xl: 48px;
  --spacing-3xl: 64px;
}
```

---

## 6. 컴포넌트 스타일

### 6.1 버튼

#### Primary Button

```css
.btn-primary {
  background: linear-gradient(135deg, #6366F1 0%, #8B5CF6 50%, #EC4899 100%);
  color: white;
  font-size: 16px;
  font-weight: 600;
  height: 56px;
  padding: 0 24px;
  border-radius: 16px;
  border: none;
  cursor: pointer;
  transition: all 300ms ease;
}

.btn-primary:hover {
  transform: scale(1.02);
  box-shadow: 0 10px 25px rgba(99, 102, 241, 0.3);
}

.btn-primary:active {
  transform: scale(0.98);
}

.btn-primary:disabled {
  background: #9CA3AF;
  cursor: not-allowed;
  opacity: 0.5;
}
```

#### Secondary Button

```css
.btn-secondary {
  background: transparent;
  color: #6366F1;
  font-size: 14px;
  font-weight: 500;
  height: 40px;
  padding: 0 16px;
  border-radius: 12px;
  border: 2px solid #6366F1;
  cursor: pointer;
  transition: all 200ms ease;
}

.btn-secondary:hover {
  background: #EEF2FF;
}
```

### 6.2 인풋

```css
.input {
  width: 100%;
  height: 56px;
  padding: 0 20px;
  font-size: 15px;
  background: rgba(255, 255, 255, 0.8);
  border: 2px solid #E5E7EB;
  border-radius: 16px;
  transition: all 200ms ease;
}

.input:focus {
  outline: none;
  border-color: #8B5CF6;
  box-shadow: 0 0 0 4px rgba(139, 92, 246, 0.1);
}

.input::placeholder {
  color: #9CA3AF;
}

.input:disabled {
  background: #F3F4F6;
  cursor: not-allowed;
}
```

### 6.3 카드

```css
.card {
  background: rgba(255, 255, 255, 0.8);
  backdrop-filter: blur(10px);
  border: 1px solid rgba(0, 0, 0, 0.05);
  border-radius: 16px;
  padding: 24px;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  transition: all 300ms cubic-bezier(0.4, 0, 0.2, 1);
}

.card:hover {
  transform: scale(1.01);
  box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
}
```

### 6.4 뱃지

```css
.badge {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  font-size: 12px;
  font-weight: 700;
  padding: 6px 12px;
  border-radius: 12px;
}

/* 카테고리별 */
.badge-housing {
  background: #DBEAFE;
  color: #1E40AF;
}

.badge-job {
  background: #DCFCE7;
  color: #15803D;
}

/* 관련도 점수 */
.badge-similarity {
  background: linear-gradient(135deg, #DCFCE7 0%, #D1FAE5 100%);
  color: #15803D;
  border: 1px solid #BBF7D0;
}
```

### 6.5 아바타

```css
.avatar {
  width: 40px;
  height: 40px;
  border-radius: 16px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

/* AI 아바타 */
.avatar-ai {
  background: linear-gradient(135deg, #6366F1 0%, #8B5CF6 50%, #EC4899 100%);
  background-size: 200% 200%;
  animation: gradient-shift 3s ease infinite;
  box-shadow: 0 4px 8px rgba(139, 92, 246, 0.3);
}

/* 사용자 아바타 */
.avatar-user {
  background: linear-gradient(135deg, #9CA3AF 0%, #6B7280 100%);
}
```

---

## 7. 모션 디자인

### 7.1 애니메이션 원칙

#### Easing Functions

```css
/* Ease Out - 자연스러운 감속 (대부분의 UI) */
--ease-out: cubic-bezier(0.4, 0, 0.2, 1);

/* Ease In - 자연스러운 가속 (사라지는 요소) */
--ease-in: cubic-bezier(0.4, 0, 1, 1);

/* Ease In Out - 부드러운 곡선 (모달, 큰 요소) */
--ease-in-out: cubic-bezier(0.4, 0, 0.6, 1);

/* Bounce - 탄력있는 움직임 (강조 효과) */
--ease-bounce: cubic-bezier(0.68, -0.55, 0.265, 1.55);
```

#### Duration

| 용도 | 시간 | 설명 |
|------|------|------|
| Instant | 100ms | 토글, 체크박스 |
| Quick | 200ms | 호버, 버튼 |
| Default | 300ms | 카드, 메시지 |
| Moderate | 500ms | 모달, 드로어 |
| Slow | 700ms | 페이지 전환 |

```css
:root {
  --duration-instant: 100ms;
  --duration-quick: 200ms;
  --duration-default: 300ms;
  --duration-moderate: 500ms;
  --duration-slow: 700ms;
}
```

### 7.2 주요 애니메이션

#### Fade In

```css
@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.fade-in {
  animation: fadeIn 600ms cubic-bezier(0.4, 0, 0.2, 1);
}
```

#### Slide Up

```css
@keyframes slideUp {
  from {
    opacity: 0;
    transform: translateY(10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.slide-up {
  animation: slideUp 300ms cubic-bezier(0.4, 0, 0.2, 1);
}
```

#### Scale

```css
@keyframes scaleIn {
  from {
    opacity: 0;
    transform: scale(0.95);
  }
  to {
    opacity: 1;
    transform: scale(1);
  }
}

.scale-in {
  animation: scaleIn 300ms cubic-bezier(0.4, 0, 0.6, 1);
}
```

#### Gradient Shift

```css
@keyframes gradient-shift {
  0%, 100% {
    background-position: 0% 50%;
  }
  50% {
    background-position: 100% 50%;
  }
}

.gradient-animate {
  background-size: 200% 200%;
  animation: gradient-shift 3s ease infinite;
}
```

#### Typing Indicator

```css
@keyframes typing {
  0%, 60%, 100% {
    transform: scale(0.8);
    opacity: 0.4;
  }
  30% {
    transform: scale(1.2);
    opacity: 1;
  }
}

.typing-dot:nth-child(1) {
  animation: typing 1.4s infinite;
}
.typing-dot:nth-child(2) {
  animation: typing 1.4s 0.2s infinite;
}
.typing-dot:nth-child(3) {
  animation: typing 1.4s 0.4s infinite;
}
```

### 7.3 호버 효과

```css
/* 카드 호버 */
.card-hover {
  transition: all 300ms cubic-bezier(0.4, 0, 0.2, 1);
}

.card-hover:hover {
  transform: translateY(-4px) scale(1.01);
  box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
}

/* 버튼 호버 */
.button-hover {
  transition: all 200ms cubic-bezier(0.4, 0, 0.2, 1);
}

.button-hover:hover {
  transform: scale(1.02);
  box-shadow: 0 10px 25px rgba(99, 102, 241, 0.3);
}

.button-hover:active {
  transform: scale(0.98);
}
```

### 7.4 로딩 애니메이션

```css
/* Spinner */
@keyframes spin {
  from {
    transform: rotate(0deg);
  }
  to {
    transform: rotate(360deg);
  }
}

.spinner {
  animation: spin 1s linear infinite;
}

/* Pulse */
@keyframes pulse {
  0%, 100% {
    opacity: 1;
  }
  50% {
    opacity: 0.5;
  }
}

.pulse {
  animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
}
```

---

## 8. 일러스트레이션

### 8.1 일러스트 스타일

#### 스타일 가이드

- **스타일**: 플랫 디자인 (Flat Design)
- **선 굵기**: 2-3px
- **색상**: 브랜드 컬러 팔레트 사용
- **그라데이션**: 부드럽게 적용
- **표정**: 친근하고 긍정적

#### 활용 케이스

- 빈 상태 (Empty State)
- 에러 페이지 (404, 500)
- 온보딩 화면
- 성공 메시지

### 8.2 이모지 사용

#### 사용 원칙

- **일관성**: 같은 의미는 같은 이모지
- **절제**: 한 문장에 1-2개만 사용
- **맥락**: 텍스트와 어울리는 이모지

#### 주요 이모지

| 이모지 | 용도 |
|--------|------|
| 👋 | 인사, 환영 |
| 🏠 | 주거 |
| 💼 | 일자리 |
| 🚀 | 창업 |
| 📚 | 교육 |
| 💰 | 자산, 돈 |
| 🎯 | 관련도, 목표 |
| ✅ | 완료, 성공 |
| 📅 | 날짜, 기간 |
| 📍 | 지역, 위치 |
| 💬 | 메시지, 대화 |
| 🔍 | 검색 |
| 📋 | 정책 |

---

## 9. 다크 모드

### 9.1 다크 모드 컬러 (향후 지원)

현재는 라이트 모드만 지원하나, 향후 다크 모드 추가 예정

#### Background

| 라이트 | 다크 | 용도 |
|--------|------|------|
| White | #111827 | 배경 |
| Gray-50 | #1F2937 | 카드 배경 |
| Gray-100 | #374151 | 강조 배경 |

#### Text

| 라이트 | 다크 | 용도 |
|--------|------|------|
| Gray-900 | White | 주요 텍스트 |
| Gray-600 | Gray-300 | 보조 텍스트 |
| Gray-500 | Gray-400 | 약한 텍스트 |

---

## 10. 디자인 체크리스트

### 10.1 디자인 전 체크리스트

- [ ] 브랜드 컬러 사용 (Indigo, Purple, Pink)
- [ ] 타이포그래피 스케일 준수
- [ ] 둥근 모서리 (16px 기본)
- [ ] 일관된 여백 (Spacing Scale)
- [ ] 호버 효과 정의
- [ ] 반응형 레이아웃
- [ ] 접근성 고려 (색상 대비, 키보드 내비게이션)

### 10.2 개발 전달 체크리스트

- [ ] 디자인 파일 (Figma / Sketch)
- [ ] 컴포넌트 명세
- [ ] 인터랙션 플로우
- [ ] 애니메이션 스펙
- [ ] 브레이크포인트
- [ ] 에셋 (아이콘, 이미지)

---

## 11. 도구 및 리소스

### 11.1 디자인 도구

- **Figma**: UI 디자인, 프로토타이핑
- **Illustrator**: 로고, 아이콘
- **After Effects**: 애니메이션

### 11.2 개발 도구

- **Tailwind CSS**: 유틸리티 기반 CSS
- **Lucide React**: 아이콘 라이브러리
- **Framer Motion**: 애니메이션 라이브러리 (선택)

### 11.3 컬러 도구

- **Coolors**: 컬러 팔레트 생성
- **ColorBox**: 접근성 확인
- **WebAIM Contrast Checker**: 색상 대비 확인

---

## 부록

### A. 컬러 변수 (CSS)

```css
:root {
  /* Primary Colors */
  --indigo-50: #EEF2FF;
  --indigo-600: #4F46E5;
  --indigo-700: #4338CA;
  
  --purple-500: #8B5CF6;
  --purple-600: #7C3AED;
  
  --pink-500: #EC4899;
  --pink-600: #DB2777;
  
  /* Functional Colors */
  --green-600: #16A34A;
  --red-600: #DC2626;
  --blue-600: #2563EB;
  --orange-500: #F97316;
  
  /* Neutrals */
  --gray-50: #F9FAFB;
  --gray-200: #E5E7EB;
  --gray-400: #9CA3AF;
  --gray-600: #4B5563;
  --gray-900: #111827;
  
  /* Spacing */
  --spacing-xs: 4px;
  --spacing-sm: 8px;
  --spacing-md: 16px;
  --spacing-lg: 24px;
  --spacing-xl: 32px;
  
  /* Border Radius */
  --radius-sm: 8px;
  --radius-md: 12px;
  --radius-lg: 16px;
  --radius-xl: 24px;
  
  /* Shadows */
  --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
  --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
  --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
  --shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
}
```

### B. Tailwind Config

```js
module.exports = {
  theme: {
    extend: {
      colors: {
        indigo: {
          50: '#EEF2FF',
          600: '#4F46E5',
          700: '#4338CA',
        },
        purple: {
          500: '#8B5CF6',
          600: '#7C3AED',
        },
        pink: {
          500: '#EC4899',
          600: '#DB2777',
        },
      },
      borderRadius: {
        'xl': '16px',
        '2xl': '24px',
      },
      fontFamily: {
        sans: ['Inter', 'sans-serif'],
      },
    },
  },
};
```

---

**문서 버전**: 1.0  
**최종 수정**: 2026년 1월  
**작성자**: 브랜드 디자인팀
